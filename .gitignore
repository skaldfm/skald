# Dependencies
/vendor/

# Build output
/skald
/bin/
/dist/

# Data directory (SQLite DB + uploads)
/data/

# Environment
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
.idea/
.vscode/

# Test
/coverage/
*.out

# Docker
docker-compose.override.yml

# Tailwind standalone CLI binary
tailwindcss

# Node (Tailwind plugins)
node_modules/
# package.json is tracked — it pins the Tailwind toolchain for the Docker/release
# CSS build (see Dockerfile). package-lock.json stays ignored (no lockfile checked in).
package-lock.json

# Tailwind output
static/css/style.css

# Temporary
/tmp/
